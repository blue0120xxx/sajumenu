# sajumenu 운영 안내서 (RUNBOOK)

돈을 버는 큰 그림은 하나예요: **좋은 글을 꾸준히 늘린다 → 검색 유입이 는다 → 광고·제휴 수익이 는다.**
봇이 도와주는 건 "글을 꾸준히 만드는 일"까지고, 실제 방문자는 검색·SNS·입소문으로 늘어나요.

---

## 1. 새 가이드 글 추가하는 법 (사람이 할 때)

1. `guide/_template.html` 을 `guide/글-주소/index.html` 로 복사해요.
   - 글 주소는 영어 소문자와 하이픈만. 예: `guide/rainy-day-menu/index.html`
2. 파일 안의 `{{ }}` 부분을 전부 바꿔요. 맨 위 `<meta name="robots" content="noindex">` 줄은 **지워요**.
3. `guide/index.html` 에 카드 한 개를 추가해요 (기존 `<a class="card" ...>` 를 복사해서 수정).
4. 방금 쓴 글 맨 아래 "함께 읽으면 좋아요"에 관련 글 링크를 넣고,
   관련된 다른 글에도 이 새 글 링크를 하나 넣어요 (서로 연결되게).
5. 루트에서 아래를 실행해 `sitemap.xml` 을 갱신해요.
   ```
   node scripts/gen-sitemap.mjs
   ```
6. 커밋 + 푸시해요.
7. (선택) 구글 서치콘솔 → URL 검사 → 새 글 주소 넣고 "색인 생성 요청".

### 글 하나 체크리스트
- [ ] 제목에 사람들이 실제로 검색할 말이 들어갔나 (예: "비 오는 날 메뉴")
- [ ] `description` 80~120자, `keywords` 5~7개
- [ ] 본문 800자 이상, 소제목(h2) 3개 이상
- [ ] `canonical`, `og:url`, JSON-LD 의 주소가 실제 주소와 같은가
- [ ] `noindex` 줄 지웠나
- [ ] `guide/index.html` 카드 추가했나
- [ ] `node scripts/gen-sitemap.mjs` 돌렸나

---

## 2. 운영봇(콘텐츠봇) — 클라우드 루틴

주 1회 새 글 **초안**을 만들어 GitHub PR 로 올려요. **자동 배포는 안 해요.** 강민성님이 PR 을 보고 합치면 배포됩니다.

- 실행 주기: 주 1회 (예: 목요일 10:00 KST)
- 비용: 루틴 1회 실행분의 사용량만. (트래픽을 만들지 않으니 광고 정책과 무관)
- 등록 방법: Claude Code 에서 `/schedule` 로 아래 프롬프트를 등록.

### 등록할 프롬프트 (그대로 복사)
```
sajumenu.com 레포(blue0120xxx/sajumenu)에 새 가이드 글 초안 1개를 PR로 올려줘.

절차:
1. guide/ 안의 기존 글 제목들을 읽고, 아직 없는 주제 중 한국에서 검색량이 있을 만한
   "메뉴 고르기 / 상황별 뭐 먹지 / 오행·음식" 주제를 하나 정한다.
2. guide/_template.html 을 복사해 guide/<영어-소문자-하이픈-주소>/index.html 을 만든다.
   맨 위 noindex 메타 줄은 지운다. {{ }} 자리표시자를 모두 실제 내용으로 채운다.
3. 본문은 한국어, 900~1500자, h2 소제목 3~4개, 표나 목록 1개 이상.
   초등학생도 이해할 쉬운 문장. 과장·거짓 정보 금지.
4. 애드센스/쿠팡 자리는 템플릿의 주석 상태 그대로 둔다 (승인 전이므로 절대 활성화하지 않는다).
   쿠팡 추천 상자는 글과 자연스럽게 어울릴 때만 "상품 종류"만 언급하고 링크는 example.com 주석 그대로 둔다.
5. guide/index.html 에 새 글 카드를 추가하고, 관련 글끼리 링크를 서로 건다.
6. node scripts/gen-sitemap.mjs 를 실행해 sitemap.xml 을 갱신한다.
7. 위 변경을 새 브랜치에 커밋하고 PR을 연다. PR 본문에 "무슨 주제를 왜 골랐는지,
   검토 시 봐야 할 점"을 한국어로 적는다.
8. main 에 직접 push 하거나 PR을 스스로 merge 하지 않는다. 보고만 한다.
```

---

## 3. 주간 리포트봇

기존 **점검봇(일요일)** 프롬프트에 아래 한 문단을 덧붙이면 따로 만들 필요 없어요.
```
추가로, 다음 수치를 표로 정리해서 보고해줘:
- GoatCounter 총 방문수와 지난 7일 방문수
  (curl "https://sajumenu.goatcounter.com/counter/TOTAL.json?start=YYYY-MM-DD&end=YYYY-MM-DD", end는 미포함)
- guide/ 글 개수, 지난주 대비 새 글 수
- (애드센스/쿠팡 승인 후) 지난주 예상 수익 — 승인 전이면 "미승인"이라고만 적기
그리고 "다음 주에 쓰면 좋을 글 주제" 3개를 제안해줘.
```

---

## 4. 애드센스 켜기 (승인 후에만)

승인 신청은 **가이드 글이 12~15개쯤 쌓이고 방문이 어느 정도 붙은 뒤**에 하는 게 통과율이 높아요.

승인되면:
1. `guide/_template.html` 과 **이미 배포된 모든 가이드 글**의 `<head>` 에서
   `애드센스: 승인 후에만 켜요` 주석 안의 `<script async src=".../adsbygoogle.js?client=ca-pub-XXXX">` 한 줄을 주석 해제.
2. `ca-pub-XXXXXXXXXXXXXXXX` 를 내 게시자 ID로 교체 (전부 동일 값).
3. "자동 광고"만 쓸 거면 여기까지. 위치를 직접 정하려면 본문의 `<ins class="adsbygoogle">` 블록 주석도 해제하고 `data-ad-slot` 을 실제 슬롯 번호로 교체.
4. `index.html`(메인)에도 같은 `<head>` 스크립트 한 줄을 넣을지 결정 — 넣으면 메인에도 광고가 붙어요.
5. 커밋 + 푸시. 광고는 몇 시간~하루 뒤부터 표시돼요.

> ⚠️ 절대 하지 말 것: 내 광고 클릭하기, 지인에게 클릭 부탁, 자동 트래픽/클릭 봇.
> 무효 트래픽으로 판단되면 계정이 **영구 정지**돼요.

---

## 5. 쿠팡 파트너스 켜기 (승인 후에만)

1. partners.coupang.com 가입 → 사이트에 `sajumenu.com` 등록 → 승인 대기(보통 1~2일).
2. 승인되면 상품별 추천 링크를 만들 수 있어요.
3. 글에 넣을 때는 템플릿의 `쿠팡 파트너스 추천 상자` 주석을 풀고
   `href="https://example.com"` 를 내 추천 링크로 교체.
4. 링크 태그는 반드시 `rel="nofollow sponsored noopener"`, 그리고 상자 안 `.disc` 고지 문구를 **지우지 말 것** (쿠팡 필수 규정).
5. 글 내용과 무관한 상품을 억지로 넣지 않기. 한 글에 1~2개면 충분.

---

## 6. 돈 들어가는 부분 (승인 필요)

| 항목 | 대략 비용 | 언제 |
|---|---|---|
| 콘텐츠봇 클라우드 루틴 실행 | 실행 1회당 사용량 (소액) | 주 1회 |
| 도메인 `sajumenu.com` 갱신 | 연 1~2만 원대 | 매년 |
| 애드센스 / 쿠팡 파트너스 가입·정산 | 무료 | - |

새 비용이 생기면 먼저 알리고, 승인 후에만 진행해요.

---

## 7. 안 하는 것 (정책·법 위반)

- 가짜 방문자·자동 클릭으로 수치 부풀리기 (애드센스 영구 정지 + 부정행위)
- 없는 정보를 지어내거나 과장한 글
- 다른 사이트 글 복사
- 제휴 링크 고지 문구 삭제
